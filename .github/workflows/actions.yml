name: SecimDataGet

on:
  workflow_dispatch:

jobs:
  AA-GetData:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3 # checkout the repository content

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10" # install the python version needed

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r Secim_2024_Haber_Kaynaklarından_Veri_Çekme/requirements.txt

      - name: execute py script # run main.py
        run: python Secim_2024_Haber_Kaynaklarından_Veri_Çekme/Scripts/aa_getdata.py
  
  TRT-GetData:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3 # checkout the repository content

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10" # install the python version needed

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r Secim_2024_Haber_Kaynaklarından_Veri_Çekme/requirements.txt

      - name: execute py script # run main.py
        run: python Secim_2024_Haber_Kaynaklarından_Veri_Çekme/Scripts/trt_getdata.py 
   
  NTV-GetData:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3 # checkout the repository content

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10" # install the python version needed

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r Secim_2024_Haber_Kaynaklarından_Veri_Çekme/requirements.txt

      - name: execute py script # run main.py
        run: python Secim_2024_Haber_Kaynaklarından_Veri_Çekme/Scripts/ntv_getdata.py
  
  haberturk-GetData:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3 # checkout the repository content

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10" # install the python version needed

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r Secim_2024_Haber_Kaynaklarından_Veri_Çekme/requirements.txt

      - name: execute py script # run main.py
        run: python Secim_2024_Haber_Kaynaklarından_Veri_Çekme/Scripts/haberturk_getdata.py

  CNN-GetData:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3 # checkout the repository content

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10" # install the python version needed

      - name: install python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r Secim_2024_Haber_Kaynaklarından_Veri_Çekme/requirements.txt

      - name: execute py script # run main.py
        run: python Secim_2024_Haber_Kaynaklarından_Veri_Çekme/Scripts/cnn_getdata.py